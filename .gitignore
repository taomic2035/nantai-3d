# ============ 素材 (用户输入, 不入库) ============
input/
photos/

# ============ 生成产物 (可重新生成, 不入库) ============
layouts/
web/data/*.ply
web/data/manifest.json
web/data/recon/
scenes/
corpus/
recon/
verification/output/

# 素材库可移植性 (见 pipeline/mock_assets.py):
#   registry.json 入库 (小、可审阅, 含 sha256), ply 由全局 *.ply 规则忽略。
#   fresh clone 用 `make assets` 从 tracked 确定性生成器还原 assets/*.ply 并自校验。
# GPT 交付目录整体不入库 (生成逻辑已迁入 pipeline/mock_assets.py 作为真相源)。
handoff/deliverables/

# ============ Python ============
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
*.egg
.eggs/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# ============ 虚拟环境 ============
.venv/
venv/
env/
ENV/

# ============ 密钥 / 配置 ============
.env
.env.local
.env.*.local
*.key
secrets.json

# ============ IDE / 编辑器 ============
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ============ 日志 / 临时 ============
*.log
logs/
tmp/
*.tmp

# ============ 大文件 / 二进制 ============
*.ply
!verification/**/*.ply
*.bin
*.pth
*.ckpt
*.safetensors
*.onnx

# ============ Node (Web 前端备选) ============
node_modules/
package-lock.json
yarn.lock
