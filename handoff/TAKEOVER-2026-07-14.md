# TAKEOVER-2026-07-14 — Codex 临时接管

状态：`READY_FOR_OPUS_REVIEW`  
接管分支：`codex/nantai-takeover`  
隔离工作区：`/Users/taomic/vibecoding/nantai-3d-codex-takeover`  
Opus 现场：`/Users/taomic/vibecoding/nantai-3d`（保留原样，未覆盖）

## What

用户于 2026-07-14 授权 Codex 在 Opus 暂停期间独立推进。Codex 把 Opus 的未提交 WIP
完整复制到隔离 worktree，在该分支完成坐标与重建契约、3DGS 属性保真、素材安全与全类型
消费、真实 Web splat layer、Studio UX、本地 adapter、模拟素材与完整验证。恢复入口为
[`FEEDBACK-TAKEOVER-001.md`](FEEDBACK-TAKEOVER-001.md)。

## Why

主工作区仅有初始 commit `b973104`，其余能力是未提交 WIP。隔离接管保护了 Opus 现场，
同时让所有补强都能在一条可审阅分支中验证；没有 stash、reset、覆盖或向 main 合并。

## Tradeoff

- 本机全链使用 synthetic mock 或标准 3DGS import；没有把 GPU 训练器、当前样例或 proxy
  宣称为实测重建。
- 裸 COLMAP 结果保持 `sfm-arbitrary / arbitrary / unaligned`；没有控制点 Sim3 就不升级
  为 ENU 米制。
- 高阶 SH 的空间旋转在正确 basis rotation 交付前 fail closed。
- Spark 依赖 CDN/WebGL；不可用时保留并明确标注 DC point fallback。
- Studio local adapter 当前只读，任务从 CLI 启动；素材文件锁使用 macOS/Linux `fcntl`。

## 接管时 8 个 P0 的关闭状态

| # | 接管问题 | 当前状态 |
|---|---|---|
| 1 | COLMAP arbitrary frame 被误标 ENU meters | **closed**：完整 `CoordinateFrame`；COLMAP 保持 arbitrary，显式 Sim3 才能升级 |
| 2 | Sim3 exactly-once 无机器约束 | **closed**：内容寻址 transform ID、PLY history、manifest ancestry/chain 一致性门禁 |
| 3 | Viewer 轴交换形成镜像 | **closed**：`(E,N,U)→(E,U,-N)`，determinant `+1`，正反映射测试 |
| 4 | `f_rest_*` 丢失且只画 point sprite | **closed**：完整 SH/属性 round-trip；Spark 2.1.0 全量 3DGS，DC fallback 诚实降级 |
| 5 | manifest/HUD 缺 provenance 与 fidelity | **closed**：schema v2、requested/actual/synthetic/frame/geometry/artifact/runtime capability |
| 6 | vegetation/prop 未真实消费 registry | **closed**：building 5 + vegetation 3 + prop 3，默认 world 11/11 实测 SHA 消费 |
| 7 | camera/grid/minimap/loading 文案硬编码 | **closed**：从 world/recon bounds、chunk size 与 manifest 动态派生 |
| 8 | 素材不可从 fresh checkout 幂等恢复 | **closed**：generator + manifest SHA + contact sheet；隔离临时目录演练通过 |

## Open

以下是明确的外部/后续工作，不是已完成能力：

1. 用真实图片+视频、COLMAP 与 GPU trainer 产生 measured 3DGS，再通过 `SplatInput` 导入。
2. 用三点以上控制点或等价证据拟合真实 SfM→ENU Sim3，并记录残差/不确定度。
3. 把 COLMAP distortion evidence 接入畸变感知训练/投影；目前完整保存但不消费。
4. 若要离线部署，需 vendoring Spark/Three/WASM；当前网络失败会降级到 DC preview。
5. 根据目标设备测量 vegetation 点预算与 3.1M-point world 的帧率/内存。

## Next

- Opus 先读 [`FEEDBACK-TAKEOVER-001.md`](FEEDBACK-TAKEOVER-001.md) 与
  [`docs/verification/2026-07-14-takeover-report.md`](../docs/verification/2026-07-14-takeover-report.md)。
- 以路径边界 review：core coordinate/reconstruction → assets/world → Viewer/Studio/docs。
- 在用户授权前，不把 takeover 分支合并到 Opus 主工作区或 main。

## Evidence

- 基线：`HEAD == origin/main == b973104e641d7aa186028164eefa5aa1d87984a7`。
- 接管前：`58 passed`；当前完整结果与浏览器证据见 verification report。
- 11 个素材幂等哈希：registry `c20e12…2144`，manifest `7df018…f33d`。
- fresh-checkout 演练：generate/register/world/reconstruct + 18 scoped tests 全部通过。
